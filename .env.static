# Static build environment variables
# These are used when building the static version for GitHub Pages

BUILD_MODE=static
NEXT_PUBLIC_BUILD_MODE=static

# URL of your main dynamic site (change this to your actual URL)
# This is shown in the static banner with a link to live data
NEXT_PUBLIC_MAIN_SITE_URL=https://your-main-site.com

# Optional: If you deploy to a subdirectory on GitHub Pages
# Uncomment and set this if deploying to username.github.io/swimmingly
# NEXT_PUBLIC_BASE_PATH=/swimmingly
